<p id="notice"><%= notice %></p>

<h1>Authors</h1>

<table class="table">
  <thead>
    <tr>
      <th scope="col">First name</th>
      <th scope="col">Last name</th>
      <th scope="col">Birthdate</th>
      <th scope="col">Nationality</th>
      <th scope="col">Category</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @authors.each do |author| %>
      <tr>
        <td scope="row"> <%= author.first_name %></td>
        <td scope="row"><%= author.last_name %></td>
        <td scope="row"><%= author.birthdate %></td>
        <td scope="row"><%= author.nationality %></td>
        <td scope="row"><%= author.category.name unless author.category.nil? %></td>
        <td scope="row"><%= link_to 'Show', author %></td>
        <td scope="row"><%= link_to 'Edit', edit_author_path(author) %></td>
        <td scope="row"><%= link_to 'Destroy', author, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Author', new_author_path %>
